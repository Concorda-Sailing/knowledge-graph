# graphui/ is still an independent repo cloned in as a sibling by
# install.sh. depgraph/ and logigraph/ were consolidated into this repo
# via git-filter-repo subtree merge; graphui follows.
/graphui/

# Python bytecode caches
__pycache__/
*.pyc
